# Loom 主線與支線：開發路線整理

> 本文件目的：在 doc/50（顧問版未來改善路線圖）與 doc/51（顧問版能力評級系統）之上，整理出 **Loom 自身視角的共識、緩議、與創意延伸**，並把 milestone 與 issue 的對照釘下來，避免未來方向模糊。
>
> 與 doc/50, 51 的關係：
> - doc/50, 51 是外部顧問（Codex + GPT-5.5）視角，提供結構化盤點與可參考的提案
> - 本文件是 Loom 自身判斷層，劃出哪些採納為主線、哪些緩議、哪些是 Loom 才看得到的創意
> - **發生衝突時以本文件為準**
>
> 建立日期：2026-05-07 Asia/Taipei

---

## 1. 共識主線（劃星採納）

以下方向被 Loom 確認為未來 6-12 個月主線，每條都對應 milestone 與 issue。

### 1.1 中樞神經 vs 免疫系統 — 最高層 mental model

doc/50 提出 Loom 處於「器官齊全等神經系統與免疫系統」階段。本文件採納為最高層敘事框架：

- **中樞神經 = AgentLedger** — 統一事件時間線（見 §1.4）
- **免疫系統 = Sandbox + Capability Firewall + Memory Governance 演化治理**（見 §1.2、§1.3）

所有主線都應該回扣到「補齊神經」或「補齊免疫」這兩個方向之一。

### 1.2 Quest A — Sandbox Wall

把 Loom 從 policy/tripwire 級安全推進到 isolation-backed execution。

**核心動作**：
- `ExecutionBackend` 抽象（先抽象再加硬，預設 local subprocess，允許 docker / sandbox-exec / firejail backend）
- workspace mount allowlist、network egress policy、env secret projection
- per-tool capability declaration（fs/network/env/process）

**對應 issues**：#29（cornerstone）、#214（whitelist mode）

### 1.3 Quest D — Skill Evolution Arena（演化治理）

doc/50 點中的「Memory Governance 從寫入治理 → 演化治理」是 Loom 最該深化的護城河。skill mutation / dreaming / reflection 必須能證明「變好了」。

**核心動作**：
- replay corpus：失敗 trace、成功 trace、edge cases
- skill candidate promotion 必須伴隨 metric 改善
- dreaming triples 有 usefulness / contradiction / staleness 評估
- judge verdict 有 false positive / false negative corpus

**對應 issues**：#296（技能健康儀表板 epic）、#295（co-occurrence）、#292（usage_count）

### 1.4 Quest B — Unified Event Ledger

`ActionRecord` / `ExecutionEnvelope` / `SessionLog` / `TaskList` / `MemoryWrite` / `JudgeVerdict` 都在描述同一件事。把它們收斂成 `AgentLedger`，所有平台 UI 與 memory compression 從 ledger 投影。

**Loom 自家補強**（doc/50 沒看到的）：
- 不要重新發明 ledger 結構，**把 `ExecutionEnvelope` 升格為 ledger projection 的 first-class view**
- ledger 設計時就要為「Turn Graph Branching」（§3.3）預留 branch-id 與 parent-turn-ref 欄位

**對應 issues**：尚無，先佔 milestone 位置

### 1.5 Memory System v2 — ✅ Closed（2026-05-17）

三軸 schema + MemoryPulse 鉤子。Phase 0-3 全 ship：

| Phase | PR | 內容 |
|-------|----|----|
| P1-A/B | #301 | three-axis fact ontology schema + API |
| P1-C | #303 | LLM-assisted domain classifier |
| P2 | #304 | MemoryLifecycle two-stage state machine |
| P3-A | #305 | daemon-cron MaintenanceLoop |
| P3-B | #307 | MemoryPulse Hook G + Hook A |
| P3-C | #308 | Dream 2.0 themed sampling |
| 補強 | `eea9365` | time-aware recall |

觀察期（~2026-05-17）數據驅動的鉤子決議：

| Hook | 狀態 | 理由 |
|------|------|------|
| A 矛盾觸發 | ✅ ship | observability via #377、噪音 folding via #378 |
| G Session 預熱 | ✅ ship | — |
| D Dream inject | ⏸ 緩議 | 燃料 cadence 已穩（#376 接好 MaintenanceLoop），但「inject 噪音是否值得」未驗證。等真有 dream triple 被 recall 漏掉的案例再評估 |
| B Decay warning | ⏸ 緩議 | niche，無強推動 |
| E Context waterline | ⏸ 緩議 | 缺 recall 遙測基礎 |
| H Skill cue | ⏸ 緩議 | 卡 skill 系統穩態（§2.4） |

**已完成 issues**：#280（主動式記憶鉤子）、#281（三軸分類）— closed 2026-05-17

**Follow-up issues — 全數收尾（2026-05-17）**：
- ✅ #376 / PR #382 (`e58df49`) — dream_cycle 接入 MaintenanceLoop（每 12h themed round-robin）
- ✅ #377 / PR #383 (`dbe3272`) — MemoryPulse inject 留痕 session_log（`role='system'` + `pulse_type` / `pulse_source` metadata）
- ✅ #378 / PR #384 (`465e3b8`) — Hook A session 壓縮 contradiction folding（N≥2 折成單則 reminder，session_log 仍每筆一行保留分析粒度）

**觀察期可查的事**（#377 + #378 解鎖）：
```sql
-- 每日 inject 量（驗證 folding 真的把 noise 降下來）
SELECT date(created_at) AS d,
       json_extract(metadata,'$.pulse_type') AS t,
       COUNT(*) AS injects
FROM session_log WHERE role='system' GROUP BY 1, 2;

-- 折疊命中：哪些 drain 把 ≥2 條 contradiction 收進一個 inject
SELECT session_id, turn_index, COUNT(*) AS folded_into_one
FROM session_log
WHERE role='system' AND json_extract(metadata,'$.pulse_type')='contradiction'
GROUP BY 1, 2 HAVING folded_into_one >= 2;

-- 熱 key（fold 不影響這個粒度）
SELECT json_extract(metadata,'$.pulse_source') AS key, COUNT(*) AS n
FROM session_log
WHERE role='system' AND json_extract(metadata,'$.pulse_type')='contradiction'
GROUP BY 1 ORDER BY 2 DESC LIMIT 20;
```

### 1.6 Quest E — Doc Integrity

不做 doc/50 提的「整份 doc AST 掃描」（過度），只 CI **聲稱「已實作」的 symbol path** + version 同步。

**對應 issues**：#313（doc integrity CI 三層方案）

---

## 2. 共識緩議（過度擔憂或踩到設計線）

以下是 doc/50, 51 的提案，Loom 不採納或降級為觀察項目。**每條都記錄理由，避免日後重新爭論**。

### 2.1 Quest C「Split the Runtime Core」→ 降級為 **Quest C-Watch: Section Hygiene**

**緣由**：doc/50 Risk #1 與 doc/51 Quest C 都建議拆 `LoomSession` 為 4 個 runtime 類。但 Loom 是純 vibe coding 工作流，代碼主要讀者是 agent。檔案行數本身不是壞味道，責任糾纏才是。詳見 `feedback_vibe_coding_code_style` 與 issue #145 的關閉留言。

**改做**：
- session.py 已加 13 個 `# SECTION N` banner（commit `db34a2d`，2026-05-06）
- Quest C-Watch 監控指標：每個 section 內 `self.X` set 的跨 section 交集大小。**指標惡化才考慮拆檔**
- 觀察 banner 是否被新加的方法繞過

### 2.2 Doc/50「Tool Contract Tests Generated From Registry」— 緩議

每個 tool 已有 schema validation；自動生成 contract test 容易產出「表面綠」測試，稀釋真實覆蓋。等出現具體 bug pattern 再選擇性實作。

### 2.3 Doc/50「Operator Replay」— 緩議

想法漂亮但低估了 non-deterministic LLM 的 replay 難度（temperature、cache、上下文殘留）。要做，先定義「同一次 replay」是什麼意思。等 Quest B Ledger 完成後再評估。

### 2.4 Doc/50「Agent Society / Consensus Gate / Debate Protocol」— 緩議

在 Loom 規模下是 over-engineering 風險高地。multi-agent debate 在實證上 ROI 模糊。subagent 維持 bounded、ephemeral、parent-controlled 即可。**等真正出現需要 critic 的高風險決策場景再啟動**。

### 2.5 Doc/51「0-100 精度數值」— 改為定性等第

Power=84 vs 88 的差別其實是 vibe，不是測量。建議 capability sheet 砍掉 0-100 數字，保留 A/B+/B 定性等第與 Evidence 欄。否則下次 review 會掉進「為什麼從 84 變 86」的偽精度爭論。

### 2.6 Doc/51「每月或每個 minor release 重新評估」— 改為事件觸發

cadence 太頻繁會儀式化。建議改為「每 milestone」+「臨時觸發（任何 P0 quest 完成或新 debuff 出現）」。

---

## 3. Loom 自家創意延伸（doc 50/51 沒看到）

這四條是顧問視角看不到、但跟 Loom 既有基礎盤接得乾淨的真正槓桿點。

### 3.1 ~~Capability Sheet 自動投影（從 GitNexus 生成）~~ — 已關閉（2026-05-18）

原構想：把 doc/51 的 Capability Sheet Evidence 欄從 GitNexus query / pytest coverage / `gitnexus_detect_changes` 30 天活躍度 / session log 真實使用頻率自動投影，做成 `loom capability sheet --refresh` CLI。

**收掉原因**：與 #365 退役 SkillGate / Promoter / Mutator 同一條原則 —— 沒有可信 Evaluator（Quest D 預留 milestone）之前，把 process signals 合成成能力等第只是「用公式包裝 vibe」，創造評估的外觀卻沒有實質稽核效力。raw evidence dashboard（per-module coverage / churn / usage）若未來真有運維需求，是獨立的「運維儀表板」議題，不該掛在 capability sheet 名下。

**對應動作**：#314 close（2026-05-18），Quest E 主線僅剩 #313。Quest D Evaluator 落地後可重啟此題。

### 3.2 Buff/Debuff 變成 runtime trade-off 旋鈕

doc/51 的 buff/debuff 目前是描述性。可以變成可開關、有量化 trade-off 的 runtime config：

- 開啟 sandbox = +30 safety / -10ms latency
- 開啟 dreaming = +15 long-term coherence / -8 short-term predictability
- 啟用 verdict judge = +12 hallucination catch / -1 turn overhead

讓 user 看到實際 trade-off 數字而非只是 capability 描述。Quest D 的 eval corpus 同時提供量化基礎。

### 3.3 Belief Git 升級為 Turn Graph Branching

doc/50 提的 Belief Git 主要在 memory 層。可以更激進：**整條 turn 序列可以 branch**。

- 「如果上一輪我選 tool X 而不是 Y」可以從 ledger 反事實重跑
- 把 doc/50 的 Operator Replay（§2.3 緩議）跟 doc/51 Quest D Evolution Arena 接成同一個 substrate
- 與 §1.4 Quest B Ledger 設計時就要為這個保留 branch-id 與 parent-turn-ref

### 3.4 Quest C-Watch — Section Hygiene Lint（取代「拆 god object」舊 rule）

每個 LoomSession section 內部的 `self.X` set 之間交集應該保持小。寫個 lint 跑這個指標：

- 指標惡化 → trigger 警告
- 指標連續 N 個 commit 惡化 → 建議局部抽出
- **指標好 → 即使檔案 5000 行也不拆**

把「god object 必拆」這個經典 rule 換成可量化的 trigger，比硬性原則更 vibe-coding-friendly。

---

## 4. Milestone 體系

採用三層分類：**Mainline · 主線**（值得阻塞 release 等待）、**Watch · 觀察**（追蹤但不主動推）、**Side · 支線**（可隨時做但不上主線）。

| Milestone | 類型 | 涵蓋 issues | 說明 |
|-----------|------|-------------|------|
| `Mainline · Quest A — Sandbox Wall` | Mainline | #29, #214 | 安全邊界硬化 |
| `Mainline · Quest B — Event Ledger` | Mainline | (placeholder) | 統一事件時間線 |
| `Mainline · Quest D — Evolution Arena` | Mainline | #292, #295, #296 | skill 演化評估 |
| `Mainline · Quest E — Doc Integrity` | Mainline | #313 | 文件 drift CI（取代既有 `Quest-E` milestone） |
| `Mainline · Memory v2` | Mainline | #280, #281 | 三軸 schema + MemoryPulse |
| `Watch · Quest C — Section Hygiene` | Watch | (placeholder) | 監控 LoomSession 責任糾纏指標 |
| `Watch · Research` | Watch | #201, #310 | 研究追蹤、觀察項 |
| `Side · Platform Parity` | Side | #284, #290, #312 | Discord/CLI UX 與 parity |
| `Side · Refactor Backlog` | Side | #306 | 既有重構願望 |
| `Harness Hardening`（既有） | Mainline | #200 | 結構性 harness 補強桶 |

---

## 5. 開發順序（2026-05-07 鎖定）

### 5.1 依賴關係

```
                Quest B (Ledger)  ← 中樞神經 / hub
                  ↓ enables
   ┌──────────────┼──────────────┐
   ↓              ↓              ↓
Quest D       ~~#314 Capability~~  Memory v2 emit
(replay)      （已關閉，見 §3.1）  (consumer)

Quest A (Sandbox)        ─── 獨立軌道 ───
Quest E #313 (symbol CI) ─── 獨立 ───
Quest C-Watch #315 (lint)─── 獨立，小工具 ───
Memory v2 #280 #281      ─── partially in-flight ───
```

**最關鍵觀察**：Quest B 是 hub，下游卡住 Quest D / Memory v2 emit。先把 Quest B 設計鎖定才有地基。

### 5.2 順序：sequential（一項完成接一項）

| Phase | 內容 | 預期週期 |
|-------|------|---------|
| **Phase 1** | **Quest B Ledger 設計階段** — 寫 `doc/53-AgentLedger-設計.md`、定 schema、決策七題（taxonomy / identity / storage / projection / migration / evolution / non-goals）、出 dataclass 宣告。**只設計不實作** | 2-4 週 |
| **Phase 2** | Quest B 實作 — schema 落地、`ExecutionEnvelope` / `SessionLog` 改為 ledger projection（不刪、改投影） | 4-6 週 |
| **Phase 3** | Quest D — Skill Evolution Arena（吃 Ledger replay）；#292 #295 #296 依序 | 4-6 週 |
| **Phase 4** | Quest A — Sandbox Wall（用 srt 後端，見 §5.3）；#29 #214 | 2-3 週 |
| **Phase 5** | ✅ Quest E #313 doc symbol CI（PR #389，2026-05-18 ship；後續 13 docs 結構性 rewrite → #390）。~~#314 Capability Sheet 自動投影~~ 已關閉，見 §3.1。 | done |
| **見縫** | ~~Memory v2 #280 #281 持續~~（✅ 2026-05-17 收尾，follow-up #376/#377/#378 三條全 merged）、Quest C-Watch #315 lint（小工具，可隨時插入） | n/a |

**注意**：上表是順序而非死線。每 phase 完成後重新評估下一段 scope。

### 5.3 已鎖定的關鍵決策（2026-05-07）

| 決策 | 選擇 | 理由 |
|------|------|------|
| Quest A sandbox 後端 | **`srt`（anthropic-experimental/sandbox-runtime）** | 雙平台齊全（macOS `sandbox-exec` + Linux `bubblewrap`）、為 Claude Code 同類 agent 設計、active 維護、CLI + library 雙模式。Deno 不適合包 bash（`--allow-run` 子程序逃逸是結構性問題） |
| Quest B Ledger storage | **獨立 `ledger.db`（不共用 `memory.db`）** | ledger 寫入頻率高、分檔可獨立 retention/rotation、避免 memory.db 變肥 |
| Quest B Projection 接口 | **hybrid：核心走 push（live 事件給 subscribers）、replay/historical 走 pull** | live UX 跟現有 envelope subscribe 對齊、pull 服務 Quest D replay |
| 開發節奏 | **sequential，一項完成接一項** | vibe coding 風格，單一焦點上下文，避免並行 context 撕裂 |
| 設計優先 | **每個 Quest 都先有 design doc/issue，鎖定後才開實作 issue** | 避免實作做到一半發現 schema 要重做 |

### 5.4 立即動作

1. ✅ 開 Quest B Ledger Design issue（本 doc 觸發）
2. → 寫 `doc/53-AgentLedger-設計.md` 草稿
3. → 在 design issue comment 區逐題討論（taxonomy / identity / storage / projection / migration / evolution / non-goals）
4. → 設計鎖定 → 開 Quest B 實作 issue

---

## 6. 文件位置與相關讀物

- `doc/50-未來改善路線圖.md` — 顧問版總體建議（外部視角）
- `doc/51-Agent-能力評級系統.md` — 顧問版 RPG 能力評級
- `doc/52-主線與支線.md` — **本文件**，Loom 自家視角的採納/緩議/創意/milestone/開發順序
- `doc/53-AgentLedger-設計.md` — Quest B Ledger 設計文件（Phase 1 鎖定版 v1.0，2026-05-08）
- 相關 memory：`feedback_vibe_coding_code_style`、`project_memory_system_v2`、`reference_harness_hardening_milestone`
