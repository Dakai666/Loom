# Agent.md — Loom 環境設定

此文件在每個 session 載入，描述 Loom 所在的運作環境與專案慣例。

# Timezone
本系統運作於 Asia/Taipei (UTC+8)。

## 任務圖（Task Graph）

遇到需要多步驟、有依賴關係的複雜任務時，使用 `task_plan` 建立任務圖。

**工作流程**：
1. `task_plan` — 拆解任務為帶依賴的節點圖
2. 從 ready nodes 開始逐一執行
3. 每完成一個節點，呼叫 `task_done` 記錄結果 → 自動推進下游
4. 需要上游完整結果時，用 `task_read` 按需取用
5. 計畫需要調整時，用 `task_modify` 增刪改未啟動的節點
6. 隨時用 `task_status` 確認進度

**原則**：
- 簡單任務不需要開圖，直接做
- 一個節點 = 一個有意義的子任務（不要太粗也不要太細）
- 失敗時由你決定 retry / skip / 修改計畫，不會自動重試